# MOSS-TTSD local inference dependencies (heavy, GPU/CUDA required)
# Install: pip install -r requirements/model.txt --extra-index-url https://download.pytorch.org/whl/cu128
#
# Note: torch/torchaudio version must match your CUDA toolkit.
# Tested with: torch==2.8.0+cu128, CUDA 12.8
# Check available builds: https://download.pytorch.org/whl/torch/

torch==2.8.0
torchaudio==2.8.0
transformers>=4.50.0
safetensors>=0.6.0
numpy>=2.2.0
orjson>=3.11.0
tqdm>=4.67.0
PyYAML>=6.0.0
einops==0.8.1
scipy>=1.16.0
librosa==0.11.0
tiktoken>=0.12.0
soundfile>=0.13.0
gradio==5.42.0
accelerate>=0.20.0
huggingface_hub>=0.34.0

# flash-attn build/runtime deps (optional; needed if flash-attn is installed separately)
psutil
packaging
ninja
setuptools
wheel

# Optional optimizations (require Triton; conflict on Windows/CPU):
# liger_kernel
# flash-attn    # Install separately: pip install flash-attn
